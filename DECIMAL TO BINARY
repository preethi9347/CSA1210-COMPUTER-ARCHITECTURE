#include<stdio.h>
int main()
{
	int n,sum=0,m=1,r;
	printf("enter a number: ");
	scanf("%d",&n);
	while(n>0)
	{
		r=n%2;
		n=n/2;
		sum=sum+(r*m);
		m=m*10;
	}
	printf("the binary number is: %d",sum);
}
